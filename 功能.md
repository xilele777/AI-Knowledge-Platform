我按“主要功能模块”全部梳理，避免逐个小文件展开导致信息过载。重点看这些模块就能覆盖项目主线。

| 模块 | 用途 | 核心逻辑 / 关键文件 | 依赖 | 边界情况或缺陷 | 面试可讲亮点 |
|---|---|---|---|---|---|
| 应用启动 | 初始化 Vue 应用 | [main.ts](f:/AI-Knowledge-Platform/src/main.ts) 创建 App，注册 Pinia、Router、Element Plus，并先初始化用户状态 | Vue、Pinia、Vue Router、Element Plus | 用户初始化失败会影响首屏路由判断；Supabase 环境缺失时只能延后到业务操作时报错 | “启动阶段先恢复用户会话，保证路由守卫拿到准确登录态。” |
| 路由与权限 | 控制页面访问 | [router/index.ts](f:/AI-Knowledge-Platform/src/router/index.ts) 定义 `/login`、`/docs`、`/knowledge`、`/chat`、`/admin`，`beforeEach` 做登录和管理员校验 | `useUserStore`、Layouts、各 View | 管理员判断依赖用户 metadata；菜单文本疑似存在编码问题 | “用路由 meta + Pinia 用户态实现前端权限分层。” |
| 用户认证 | 注册、登录、登出、恢复会话 | [api/auth.ts](f:/AI-Knowledge-Platform/src/api/auth.ts) 封装 Supabase Auth；[stores/user.ts](f:/AI-Knowledge-Platform/src/stores/user.ts) 保存 `user/session/role` | Supabase Auth、Pinia | Auth listener 全局只绑定一次；角色来源较多，后端权限仍需 RLS 保底 | “把 Supabase Auth 封成 store，统一管理登录态和角色态。” |
| 主布局 | 普通用户页面框架 | [layouts/MainLayout.vue](f:/AI-Knowledge-Platform/src/layouts/MainLayout.vue) 承载主菜单和内容区域 | `router/menus.ts`、Element Plus | 菜单文案读取到乱码，建议确认 UTF-8 编码 | “普通业务页统一走 MainLayout，页面职责更清晰。” |
| 管理布局 | 后台页面框架 | [layouts/AdminLayout.vue](f:/AI-Knowledge-Platform/src/layouts/AdminLayout.vue) 承载后台菜单和管理页面 | admin routes、Element Plus | 前端拦截不能替代数据库权限 | “后台入口独立布局，并通过路由守卫限制 admin 访问。” |
| 文档列表 | 查看、搜索、创建文档 | [views/docs/DocsListView.vue](f:/AI-Knowledge-Platform/src/views/docs/DocsListView.vue)，[api/documents.ts](f:/AI-Knowledge-Platform/src/api/documents.ts) 的 `getMyDocuments/createDocument` | Supabase `documents` 表、`DocumentCard` | 搜索主要按标题；分页能力取决于调用方是否传 `limit/offset` | “文档模块是知识库和 AI 写作的内容入口。” |
| 文档编辑器 | Markdown 编辑、保存、分享、同步知识库 | [views/docs/DocEditorView.vue](f:/AI-Knowledge-Platform/src/views/docs/DocEditorView.vue)，`updateDocument`、`addDocumentToKnowledgeBase` | `md-editor-v3`、文档 API、知识库 API、草稿工具 | 草稿清理逻辑需重点确认；同步知识库时空内容会失败 | “文档不仅可编辑，还能一键切片进入知识库形成 RAG 数据源。” |
| AI 写作助手 | 对文档内容润色、扩写、总结、续写 | [components/document/AIAssistantPanel.vue](f:/AI-Knowledge-Platform/src/components/document/AIAssistantPanel.vue)，[utils/aiAssistantPrompts.ts](f:/AI-Knowledge-Platform/src/utils/aiAssistantPrompts.ts) | `api/ai.ts`、`stores/aiConfig.ts` | 依赖用户 AI Key；选中文本为空或模型失败时要有 UI 兜底 | “把 AI 能力嵌入编辑流程，提升文档生产效率。” |
| 本地草稿 | 防止编辑内容丢失 | [utils/documentDraft.ts](f:/AI-Knowledge-Platform/src/utils/documentDraft.ts) 用 `localStorage` 读写 `doc_draft_v1:{id}` | 浏览器 localStorage | 多端不同步；保存成功后如果没清理会出现旧草稿覆盖风险 | “用轻量本地草稿降低编辑器丢稿风险。” |
| 知识库列表 | 创建和查看知识库 | [views/knowledge/KnowledgeListView.vue](f:/AI-Knowledge-Platform/src/views/knowledge/KnowledgeListView.vue)，[api/knowledge.ts](f:/AI-Knowledge-Platform/src/api/knowledge.ts) | Supabase `knowledge_bases` 表 | 名称为空、重复命名、归档状态需要 UI 约束 | “知识库作为文档、文件和问答的组织容器。” |
| 知识库详情 | 管理文件、文档来源、QA 配置 | [views/knowledge/KnowledgeDetailView.vue](f:/AI-Knowledge-Platform/src/views/knowledge/KnowledgeDetailView.vue) | `KnowledgeFileList`、`KnowledgeDocumentSourceList`、知识库 API | QA 配置有默认/自定义两套 AI 配置，容易混淆 | “知识库详情把数据源管理和问答策略配置放在同一上下文。” |
| 文件上传 | 上传资料并写入知识库 | [components/knowledge/FileUpload.vue](f:/AI-Knowledge-Platform/src/components/knowledge/FileUpload.vue) | Supabase Storage/DB、`createKnowledgeFile`、切片写入逻辑 | 大文件、非文本文件、解析失败、重复上传需要处理 | “支持用户把外部资料转成可检索知识切片。” |
| 文本切片 | 把长文本拆成知识片段 | [utils/chunkText.ts](f:/AI-Knowledge-Platform/src/utils/chunkText.ts) 先段落拆分，再句子拆分，超长硬切，最后合并短片段 | 文档同步、文件处理 | 当前按字符长度估算，不是真 token；中文标点正则需确认编码 | “实现了面向 RAG 的文本预处理管线。” |
| 知识切片入库 | 批量写入知识库内容 | [api/knowledge.ts](f:/AI-Knowledge-Platform/src/api/knowledge.ts) 的 `batchInsertKnowledgeChunks` | Supabase `knowledge_chunks`、可选 Embedding API | `generateEmbeddings` 只有同时传 `config` 才会生成向量；否则不会写 embedding | “切片写库时预留了向量化能力，支持后续语义检索扩展。” |
| 向量工具 | Embedding 和相似度计算 | [utils/vectorEmbedding.ts](f:/AI-Knowledge-Platform/src/utils/vectorEmbedding.ts) 调 `/embeddings`，`cosineSimilarity`，`findTopSimilarChunks` | OpenAI 兼容 Embedding API | 批量生成是串行请求；模型字段同时用于聊天和 embedding 可能不匹配 | “具备从关键词检索升级到向量检索的基础能力。” |
| 聊天问答 | 用户基于知识库提问 | [views/chat/ChatView.vue](f:/AI-Knowledge-Platform/src/views/chat/ChatView.vue)，[api/chat.ts](f:/AI-Knowledge-Platform/src/api/chat.ts) | Chat 组件、AI API、知识库切片、Pinia AI 配置 | 空知识库、无命中、流式中断、消息保存失败都要处理 | “问答链路完整串起会话、检索、Prompt、流式回答和来源引用。” |
| 聊天输入 | 发送问题 | [views/chat/components/ChatInput.vue](f:/AI-Knowledge-Platform/src/views/chat/components/ChatInput.vue) | ChatView 回调、Element Plus | 空输入、连续发送、请求中重复提交 | “输入组件独立，便于控制发送状态和快捷交互。” |
| 消息列表 | 展示用户问题和 AI 回答 | [views/chat/components/ChatMessageList.vue](f:/AI-Knowledge-Platform/src/views/chat/components/ChatMessageList.vue) | Chat message 类型、SourceChunks | 流式文本更新频繁可能带来渲染压力 | “消息展示与业务请求解耦，适合扩展状态和来源引用。” |
| 来源片段 | 展示回答引用依据 | [views/chat/components/SourceChunks.vue](f:/AI-Knowledge-Platform/src/views/chat/components/SourceChunks.vue) | `ChatSourceChunk` | 切片内容过长、来源文件已删除、分数为空时需要兜底 | “让 AI 回答可追溯，增强知识库问答可信度。” |
| 关键词检索 | 从切片中找相关内容 | [utils/retrieveChunks.ts](f:/AI-Knowledge-Platform/src/utils/retrieveChunks.ts) 分词、中文 ngram、停用词过滤、打分排序 | ChatView / 问答 Prompt | 语义相近但关键词不同会漏召回；停用词文本疑似乱码 | “不用后端向量库也能实现轻量本地 TopK 检索。” |
| Prompt 构建 | 生成 AI 问答提示词 | [utils/buildQaPrompt.ts](f:/AI-Knowledge-Platform/src/utils/buildQaPrompt.ts) 构建通用 AI Prompt 和知识增强 Prompt | 检索结果、知识库 QA 配置 | Prompt 文案读取到乱码，需确认真实文件编码；严格知识模式需防幻觉 | “将检索片段、回答风格和系统约束统一封装成 Prompt 模板。” |
| AI 请求 | 调用 OpenAI 兼容接口 | [api/ai.ts](f:/AI-Knowledge-Platform/src/api/ai.ts) 的 `generateAiText`、`generateAiTextStream` | `fetch`、用户 AI 配置 | API Key 暴露在浏览器；SSE 兼容不同服务商但仍可能解析失败 | “支持普通和 SSE 流式生成，前端可实时渲染回答。” |
| 用户 AI 配置 | 保存用户自己的模型配置 | [api/userAiConfig.ts](f:/AI-Knowledge-Platform/src/api/userAiConfig.ts)，[stores/aiConfig.ts](f:/AI-Knowledge-Platform/src/stores/aiConfig.ts) | Supabase `user_ai_config`、Pinia | API Key 存前端可读表，安全性依赖 RLS；模型默认值和 embedding 模型可能冲突 | “支持每个用户自带模型服务，平台不强绑定单一供应商。” |
| 共享广场 | 展示公开分享文档 | [views/shared/SharedListView.vue](f:/AI-Knowledge-Platform/src/views/shared/SharedListView.vue)，[views/shared/SharedDetailView.vue](f:/AI-Knowledge-Platform/src/views/shared/SharedDetailView.vue) | 文档 `is_shared/shared_at` 字段 | 分享撤回、作者信息、权限边界要靠数据库策略保证 | “通过文档分享状态扩展出轻量内容社区能力。” |
| 管理后台 | 管理用户、文档、文件、问答 | [views/admin](f:/AI-Knowledge-Platform/src/views/admin)，[api/admin.ts](f:/AI-Knowledge-Platform/src/api/admin.ts) | Supabase 表、RPC、ECharts | 前端 admin 判断不够，删除类操作必须依赖数据库权限 | “后台模块覆盖内容治理和运营统计，形成平台闭环。” |
| 统计埋点 | 记录登录、文档、AI 调用等事件 | [utils/tracker.ts](f:/AI-Knowledge-Platform/src/utils/tracker.ts)，[constants/analyticsEvents.ts](f:/AI-Knowledge-Platform/src/constants/analyticsEvents.ts) | Supabase `analytics_events` | 未登录不记录；写入失败静默 warn；事件命名需长期稳定 | “用统一事件常量和轻量 tracker 支撑后续数据分析。” |
| 样式主题 | 统一视觉风格 | [styles/theme.css](f:/AI-Knowledge-Platform/src/styles/theme.css)，[styles/element-plus-overrides.css](f:/AI-Knowledge-Platform/src/styles/element-plus-overrides.css) | Element Plus、全局 CSS | 覆盖样式过多时升级 Element Plus 可能受影响 | “基于 Element Plus 做主题覆盖，快速建立一致 UI。” |

**最值得重点讲的主线：**

`登录鉴权 → 文档创建/编辑 → 文档切片 → 写入知识库 → 聊天检索切片 → 构建 Prompt → AI 流式回答 → 保存消息和引用来源`

这一条链路能体现你项目的完整度：既有业务数据管理，也有 RAG 问答、流式 AI、权限控制和管理后台。